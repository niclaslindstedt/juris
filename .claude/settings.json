{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; echo \"$f\" | grep -qE '(src/|tests/)' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PostToolUse\",\"additionalContext\":\"A new file was created under src/ or tests/. If this changes the project structure, remember to update CLAUDE.md to reflect the new structure.\"}}' || true; }",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
